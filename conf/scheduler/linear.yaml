# @package model 

defaults:
  - none 
  - _self_

scheduler:
  _partial_: True
  _target_: torch.optim.lr_scheduler.LinearLR
  total_iters: ${trainer.max_epochs} 
  start_factor: 1.0
  end_factor: 5e-01